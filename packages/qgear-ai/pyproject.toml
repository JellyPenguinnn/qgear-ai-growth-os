[project]
name = "qgear-ai"
version = "0.1.0"
description = "Prompt and evidence helpers for Q-GEAR AI Growth OS."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
